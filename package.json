{
  "name": "email-playwright-ecommerce",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:report": "npx playwright test --reporter=html",
    "send-report": "node send-playwright-report.js",
    "test:ci": "npx playwright test --reporter=html && node send-playwright-report.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  },
  "dependencies": {
    "nodemailer": "^6.9.0"
  }
}
